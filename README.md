# Humble Price Index

A self-contained HTML dashboard tracking raw material and packaging price trends for The Humble Co. The dashboard auto-updates every Monday from a scheduled Claude routine, and `index.html` embeds all of its own data (no external API calls or dependencies required to view it).
